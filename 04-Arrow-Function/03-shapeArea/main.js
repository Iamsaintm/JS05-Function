// บรรทัดที่มี \* , \*\*ให้ผลลัพธ์เป็นอะไร เพราะอะไร

// ```js
// const square = (n) => {
//   n ** 2;
// };

// console.log(square(7)); // *
// ```

// ```js
// const calcCircleArea = (r) => Math.PI * r ** 2;
// console.log(calcCircleArea(3)); // **
// ```

// * undefined เพราะไม่ใส่ return
// ** 22.7*9 = 28.27 เพราะ console.log แทนการใช้ return
